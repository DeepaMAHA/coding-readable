#include<stdio.h>
int repeated(int x[100,int n);
int main()
{
int a[100],i,n;
scanf("%d",&n);
for(i=0;i<n;i++)
scanf("%d",&a[i]);
repeated(a[i],n);
if(f==1)
printf("true");
else
printf("false");
return 0;
}
int repeated(int a[i],int n)
{
for(i=0;i<n;i++)
{
if(a[i]==0)
f=0;
else
f=1;
return f;
}
